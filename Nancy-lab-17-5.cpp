#include <iostream>   
#include <cmath>
using namespace std;

int main()
{
	int n, k, o1,o2,03 i,j,l, m, max;
	cout << "Enter N" << endl;
	cin >> n;
	max = -1;
	int* x = new int[n];
	int* y = new int[n];
	for (i = 0;i < n;i++) {
		cout << "Enter a[" << i + 1 << "] x and y  ";
		cin >> x[i] >> y[i];
	}
	for (i = 0;i < n;i++) {
		cout << "A[" << x[i] << ";" << y[i] << "]" << endl;
	}
	for (i = 0;i < n;i++) {
		for (j = 0; j < n;j++) {
			if (i != j) {

				for (l = 0; l < n;l++) {

				}
			}
		}
	}